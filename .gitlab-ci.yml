image: ubuntu

stages:
 - build

job:Abache2_deploy:
  stage: build
  before_script:
    - DEBIAN_FRONTEND=noninteractive apt-get update -yqq
  script:
    - DEBIAN_FRONTEND=noninteractive apt-get install apache2 curl -yqq
    - apache2ctl configtest
    - curl http://172.17.0.3
